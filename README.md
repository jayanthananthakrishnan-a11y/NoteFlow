# NoteFlow — Frontend

Lightweight React + Vite frontend scaffold for NoteFlow.

Quick start

1. Install dependencies

```bash
npm install
```

2. Run dev server

```bash
npm run dev
```

Features
- React + Vite
- Tailwind CSS with dark mode (class strategy)
- Pages: Home, Search
- Responsive, card-based layout
- Routing via `react-router-dom`

Next steps: connect to backend APIs for live data, add upload/preview flows.
